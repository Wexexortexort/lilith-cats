services:
  - type: web
    name: lilith-cats
    runtime: node
    plan: free
    buildCommand: npm install && npm run build
    startCommand: npx serve dist -l $PORT
    envVars:
      - key: NODE_VERSION
        value: 22
